# Generated by Cargo
# Lines starting with `#` are comments.
# Ignore compiled artifacts and build outputs
/target/

# Ignore Cargo.lock if you are building a library, but commit it for binaries
# Como este é um binário (aplicativo), geralmente é bom manter o Cargo.lock
# Se fosse uma biblioteca, você descomentaria a linha abaixo:
# Cargo.lock

# Ignore editor configuration files
.idea/
.vscode/
*.iml
*.sublime-project
*.sublime-workspace

# Ignore OS-specific files
.DS_Store
Thumbs.db

# Ignore environment variables file (if you use one)
.env

# Ignore build artifacts from PKGBUILD (Arch Linux)
*.pkg.tar.zst
pkg/
src/ # Cuidado: Se você tiver código fonte dentro de 'src' no diretório de build do makepkg

# Ignore build artifacts from Flatpak (if used)
build-dir/
.flatpak-builder/

# Ignore build artifacts from cargo-deb (if used)
# A pasta target/debian/ já está coberta por /target/
